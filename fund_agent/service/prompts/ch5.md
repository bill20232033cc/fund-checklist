<!-- version: 2.0 -->
请基于上述数据和阶段判定规则，写一段「当前阶段与关键变化」分析。

【数据验证规则 - 必须严格遵守】
在输出任何结论之前，你必须逐条核对数据表：
1. 先列出数据表中每个年份的权益投资金额原始数字（原样引用，不缩写）
2. 逐年比较，确认是增长还是下降（例如：2022→2023 是增长还是下降？）
3. 只有在逐条核对后，才能输出阶段判定结论
4. 如果数据表显示某年下降，你不得声称"逐年增长"

【口径强调 - 必须严格遵守】
数据表中有两个不同口径，不可混淆：
- **权益投资规模变动（代理指标）**：年报资产配置中权益投资金额同比变动，仅供参考，不用于阈值判定
- **规模变动阈值判定**：必须使用「份额×净值同比」口径，数据表中如有预计算结果则直接引用
- 如果「份额×净值同比」数据不可用，必须明确声明"规模变动阈值无法判定（口径数据缺失）"，不得用权益投资规模替代

【阶段判定 - 逐步核对】
按以下顺序逐步检查，每一步都必须引用数据表中的具体数字：
1. 建仓期：基金成立是否 < 2 年？
2. 转型期：是否有基金经理变更 / 投资目标变更 / 业绩基准变更 / 基金类型变更？
3. 膨胀期：份额×净值同比是否 > 50%？
4. 萎缩期：份额×净值同比是否 < -30%？
5. 以上均不满足 → 稳定期

每一步必须写出：检查了什么数据 → 结果是什么 → 是否触发。

【关键变化筛选】
从持仓变动/规模变动/费率变动3个维度筛选，只列阈值触发的项：
- 持仓变动：前十大持仓换手 > 40%（引用原始数字）
- 规模变动：份额×净值同比 > 30%（引用原始数字；如口径数据缺失则声明）
- 费率变动：管理费/托管费绝对值变动 > 0.1%（引用原始数字）

【投资假设影响】
- 对比 Ch7 信号评分方向是否逆转（如数据表中有信号评分数据则引用；如无则声明数据缺失）
- 不得凭空推断信号评分

【跟踪变量】
- 给出最该跟踪的 1-3 个变量
- 每个变量必须说明：为什么盯它？向好/向坏会改变什么判断？

章节边界：使用数据表中的预计算指标，禁止自行计算。

must_answer 字段：
- 当阶段判定结果（转型期/建仓期/膨胀期/萎缩期/稳定期）
- 判定依据（逐条引用数据）
- 关键变化（触发阈值的维度，引用原始数字）
- 是否影响原始投资假设
- 接下来最该跟踪的1-3个变量

{{ must_answer_schema }}
