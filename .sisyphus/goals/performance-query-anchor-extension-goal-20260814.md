# Goal Command（可直接发送）

```
/goal 按 .sisyphus/plans/performance-query-anchor-extension-slice-20260814.md 实施「performance 类查询受控表锚点注入收口」slice（第4个任务 = docs/research/dayu-agent-r-research-20260810.md §5 建议 4；plan 已于 2026-08-14 设计，Mimo plan review NEEDS_FIX 1 项经 controller 直接实证核验为误报——「超额表现」包含「超额」子串，设计口径不变，plan 已补匹配 alias 注释；真源已同步 docs/design.md 受控表锚点段与 docs/implementation-control.md）。只走 CIC-lite implement -> tests -> diff review。实施内容（仅 registry aliases 扩展）：fund_agent/service/extraction.py 的 DISCLOSURE_LOCATOR_CONTRACT_REGISTRY 中 performance_returns contract 的 aliases 增加 "超额收益"、"超额收益率"、"超额"、"净值表现"（匹配机制为子串包含 any(alias in query for alias in contract.aliases)；candidate_queries / acceptable_title_family / anchor_title_family / requires_table_citation 全部不变；复用既有 3.2.1 表锚点与 chat_service.py 的 _ANCHOR_PROFILE_NAMES，chat_service.py 不改）。allowed write set 严格按 plan（7 修改：fund_agent/service/extraction.py（仅 registry aliases）、tests/fund/service/test_extraction.py、tests/fund/service/test_chat_service.py、tests/fund/test_e2e_regression.py、docs/design.md、docs/implementation-control.md、tests/README.md（如验证命令变化，可选）），禁止修改 AGENTS.md / fund_agent/agent/ / fund_agent/fund/ / fund_agent/host/ / fund_agent/cli/ / fund_agent/service/models.py / scene.md / FailureCode / DocumentToolError / 10F/10G/11A 契约；不新增 failure taxonomy / profile / CLI 参数 / 依赖；不 commit / 不 push。测试：tests/fund/service/test_extraction.py 正例 _route_plan_for_query("超额收益是多少")、("超额收益率")、("超额表现")、("净值表现如何") -> profile_name == performance_returns（注：「超额表现」经「超额」子串 alias 命中，非「超额收益/超额收益率」）；负例 ("基金规模")、("持仓明细")、("管理费")、("前十大持仓") 不误命中 performance_returns；aliases 扩展后 _validated_locator_contracts() 不抛 schema_drift。tests/fund/service/test_chat_service.py retrieval contribution 用例：超额收益 query -> 含「已识别披露主题: performance_returns」+「候选表锚点」注入（复用既有 fake tool service 锚点 fixture，断言锚点行存在或按既有模式）。tests/fund/test_e2e_regression.py 真实 fixture smoke（.fund_e2e_004393 / .fund_checklist_005680 缺失 skip，沿用既有 skip 模式）。验收命令：uv run pytest tests/fund/service/test_extraction.py tests/fund/service/test_chat_service.py -k "route or anchor or performance or query" -v --tb=short；uv run pytest tests/fund/test_e2e_regression.py -v --tb=short；AGENTS.md 最小验证集 uv run pytest tests/fund/document_tools tests/fund/agent/test_minimal_tool_loop.py tests/fund/cli/test_cli.py -v --tb=short；全部通过 + git diff --check 干净。输出交接报告（changed files / diff 摘要 / 实际测试命令与输出片段）。
```

## Goal

- goal_id: `performance-query-anchor-extension-20260814`
- 目标：实施「performance 类查询受控表锚点注入收口」（第4个任务 = dayu-agent-r 研究 §5 建议 4）：`performance_returns` registry aliases 扩展「超额收益/超额收益率/超额/净值表现」，使净值增长率/基准收益率/超额收益/净值表现词面全部进入受控路由并复用既有 3.2.1 表锚点。
- 前置条件：`.sisyphus/plans/performance-query-anchor-extension-slice-20260814.md` 已设计（2026-08-14，实证 + Mimo plan review，唯一 NEEDS_FIX 项经 controller 核验为误报）。
- 设计来源：`.sisyphus/plans/performance-query-anchor-extension-slice-20260814.md`（唯一计划真源）。
- 日期：2026-08-14
