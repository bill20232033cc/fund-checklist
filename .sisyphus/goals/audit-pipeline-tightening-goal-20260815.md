# Goal Command（可直接发送）

```
/goal 按 .sisyphus/plans/audit-pipeline-tightening-slice-20260815.md 实施「审计管道设计收紧（critical 阻断 + 装配审计）」slice（来源：F1-F4 收口报告遗留建议两项裁决项；口径已于 2026-08-15 经用户拍板确认，全部按推荐：D1 critical 阻断含数据不足模式（只降门槛不豁免 critical，「数据完整性声明」场景 data_table 非空不误触 P1）；D2 LLM 审计 critical 与程序化 critical 同等阻断（误报代价 ≤3 次 regenerate 后模板降级，有界）；D3 装配违反 fail-closed 复用 schema_drift，内容为空仅 warning；D4 校验三处全接（快照×2 + 年报 LLM/模板路径，三处硬编码标题已与三模板 chapter_titles 逐字一致无误报风险））。只走 CIC-lite implement -> tests -> diff review。实施内容：① fund_agent/service/audit_pipeline.py A1——提取纯函数 _passes_audit(final_score, score_pass, violations) -> bool（语义：final_score >= score_pass 且无 critical）；_generate_and_audit_chapter 通过分支改调 _passes_audit，有 critical 时不通过且跳过 PATCH 直接走 REGENERATE（与 select_repair_strategy 语义对齐：critical 只能 regenerate 不能 patch）；AuditDecision.recommendation 同步为 has_critical -> "regenerate"；select_repair_strategy 改 score >= SCORE_PASS 且有 critical -> "regenerate"（reason 列出 critical codes），无 critical 仍 skip；顺手修正 SCORE_PASS_DEGRADED 注释「≥70分通过」->「≥75分通过」；② audit_pipeline.py A2——新增纯函数 verify_report_assembly(template, chapters) -> (ok: bool, problems: list[str])：章节集合 == set(template.chapter_ids)（缺章/多章 fail）、顺序 == sorted(template.chapter_ids)（展示顺序 fail）、每章 title == template.chapter_titles[cid]（标题与 manifest 一致 fail）、内容为空仅 warning；③ fund_agent/service/extraction.py A2 三处装配点接入校验：generate_snapshot_report（约 2834 后）、generate_report LLM 路径（约 2654 后）、_generate_chapters 模板路径（约 3710 后）；违反 -> 返回 ToolFailure(code=schema_drift, message=校验明细)（不新增 failure code；模板模式同样生效，属程序化检查）。allowed write set 严格按 plan（7 文件：fund_agent/service/audit_pipeline.py、fund_agent/service/extraction.py、tests/fund/service/test_audit_pipeline.py、tests/fund/service/test_snapshot_report_assembly.py、docs/design.md、docs/implementation-control.md、AGENTS.md），禁止动 CLI / prompts / registry / public tool 契约 / failure taxonomy / models.py / scene.md / repairer prompt「禁止修改数据表格」语义 / 修复环上限（MAX_PATCH/REGENERATE_ATTEMPTS=3）/ 模板模式接入逐章审计管道（候选 A 范围外）/ 不 commit / 不 push。测试：tests/fund/service/test_audit_pipeline.py——select_repair_strategy score=87.4 + critical -> regenerate（旧逻辑 skip 会失败，可证伪）、score=80 无 critical -> 仍 skip（既有测试保留）、_passes_audit 纯函数三态（≥门槛无 critical True / ≥门槛有 critical False / <门槛 False）、集成用例 fake LLM 持续返回高分+critical -> 章节走 regenerate -> 耗尽 -> 模板降级 passed_with_degradation（断言最终状态非 "passed"）；tests/fund/service/test_snapshot_report_assembly.py——verify_report_assembly 纯函数（正确装配 pass / 乱序 fail / 缺章 fail / 多章 fail / 标题不符 fail / 空内容 warning 不 fail）、集成用例注入缺章或乱序 chapter_contents -> generate_snapshot_report 返回 schema_drift 失败（当前代码无校验会照常产出，测试可证伪旧行为）。验收命令：uv run pytest tests/fund/service/test_audit_pipeline.py -q --tb=short；uv run pytest tests/fund/service/test_snapshot_report_assembly.py -q --tb=short；uv run pytest tests/fund/service/test_snapshot_template.py tests/fund/service/test_snapshot_extraction.py -q --tb=short；AGENTS.md 最小验证集 uv run pytest tests/fund/document_tools tests/fund/agent/test_minimal_tool_loop.py tests/fund/cli/test_cli.py -q --tb=short；全部通过 + git diff --check 干净。文档同步按 plan：docs/design.md §6.25 追加第 20 项裁决（通过判据 = 加权分数达门槛 且 无 critical；报告级装配审计集合/顺序/标题与模板 manifest 一致，违反 fail-closed schema_drift，内容为空仅 warning）、docs/implementation-control.md 快照进度节或审计收紧节追加本 slice 记录、AGENTS.md 审计规则追加「禁止高分放行 critical」与「报告装配必须经模板 manifest 校验」。输出交接报告（changed files / diff 摘要 / 实际测试命令与输出 / 旧行为可证伪说明）。
```

## Goal

- goal_id: `audit-pipeline-tightening-20260815`
- 目标：实施「审计管道设计收紧」：① critical violation 一律阻断通过（不因加权分数 ≥80/75 放行，含数据不足模式；有 critical 走 regenerate 不走 patch）；② 新增报告级装配审计（章节集合/顺序/标题与模板 manifest 一致，违反 fail-closed schema_drift，内容为空仅 warning），三处装配点全接（快照 ×2 + 年报 LLM/模板路径）。
- 前置条件：`.sisyphus/plans/audit-pipeline-tightening-slice-20260815.md` 已设计（2026-08-15，代码实证 6 项 + 测试可证伪设计）；口径 D1-D4 用户已拍板确认（全部按推荐）。
- 设计来源：`.sisyphus/plans/audit-pipeline-tightening-slice-20260815.md`（唯一计划真源）。
- 日期：2026-08-15
