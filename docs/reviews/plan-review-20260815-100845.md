# Plan Review: F1-F4 快照修复 slice

## Reviewed Target

- **Plan**: `.sisyphus/plans/f1-f4-snapshot-fix-20260815.md`
- **Scope**: 4 项 deepreview findings 全部修复（F1 严重 / F2 高 / F3 中 / F4 低）
- **Source of truth**: `docs/reviews/code-review-20260815-094136.md`

## Assumptions Tested

1. F1-F4 四项 findings 在代码中真实存在 — **已验证**（grep + read 确认）
2. `_extract_text_field` 缺失时返回 `（{label}未披露）` 降级文案 — **已验证**（line 723）
3. `_snapshot_common_header` 接收 `period` 但未透传给 `_period_label` — **已验证**（line 39 vs 47）
4. `generate_snapshot_template_chapter` 未从 kwargs 读取 `period` — **已验证**（line 301-305 无 period）
5. `range(1, 7)` 是唯一未使用 `front_chapter_ids` 的位置 — **已验证**（lines 2135/2138/2148/2195 已用模板属性）
6. 现有测试已有 `_FakeToolService` 基础设施 — **已验证**（`test_snapshot_report_assembly.py:50`）

## Findings

无 material findings。以下为 2 个 implementation nit（不阻塞）：

### N1-未修复-[低]-F1 测试建议新增 _FakeStore 而非复用已有 _FakeToolService 模式

- **位置**: Plan F1 测试设计
- **问题类型**: 最佳实践偏离
- **当前写法**: plan 提议新增 duck-typed `_FakeStore`（实现 `search` / `list_tables` / `read_table`），通过 `FundDocumentToolService({document_id: store})` 包装
- **反例/失败场景**: 不会失败，但与现有测试模式不一致
- **为什么有问题**: `test_snapshot_report_assembly.py` 已有 `_FakeToolService`（line 50），直接 duck-type 模拟 tool service 接口（`search_document` / `read_table` / `list_tables`），所有 extraction 函数均接受该 duck-type。新增 store 级 fake 增加间接层，偏离现有约定
- **直接证据**: `test_snapshot_report_assembly.py:50-64` 定义 `_FakeToolService`，`snapshot_extraction.py:718-724` `_extract_text_field` 仅调用 `tool_service.search_document`
- **影响**: 无功能影响；代码风格不一致，后续维护者需理解两种 fake 模式
- **建议改法和验证点**: 直接复用 `_FakeToolService` 模式，`search_map` 中按 query 键返回不同 excerpt 即可；或保留 plan 方案但注明与现有模式有意不同
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低（nit，不阻塞）

### N2-未修复-[低]-F3 generate_snapshot_template_chapter 未显式读取 period kwargs

- **位置**: Plan F3 设计第三行
- **问题类型**: 范围漂移
- **当前写法**: plan 写「generate_snapshot_template_chapter：从 kwargs 读取 period，两处 _period_label 调用透传」
- **反例/失败场景**: 实现 agent 可能遗漏：`generate_snapshot_template_chapter`（line 290）当前不读 `period`，需要新增 `period = kwargs.get("period")` 并在 line 320/325 透传
- **为什么有问题**: plan 措辞暗示 `period` 已在 kwargs 中但未透传，实际需要先新增读取再透传。不算歧义但实现 agent 需注意
- **直接证据**: `snapshot_generator.py:301-305` 读取 chapter_id/fund_name/report_year/quarter/snapshot_data，无 period；line 320/325 调用 `_period_label(template_id, report_year, ...)` 无 period
- **影响**: 低——实现 agent 看到 plan 中两处 `_period_label` 调用应能自行补上 `period` 读取
- **建议改法和验证点**: plan 可更明确写「新增 `period = kwargs.get("period")`，line 320/325 的 `_period_label` 调用增加 `period=period`」
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低（nit，不阻塞）

## Open Questions

无。

## Residual Risks

1. **文本回退路径无测试覆盖** — plan F2 已明确新增纯文本回退测试，此 residual risk 将消除。
2. **risk_notes 无独立测试** — plan F1 已明确新增断言 `risk_notes != single_investor_20pct`，此 residual risk 将消除。
3. **模板模式端到端 Markdown 未测** — plan 非目标明确排除，另立 slice。
4. **LLM 模式审计管道快照集成未测** — plan 非目标明确排除，另立 slice。

Residual risks 3/4 与 deepreview artifact 一致，plan 明确排除并承诺另立 slice，合理。

## Self-Check

- [x] reviewed target、scope、source of truth、assumptions tested 已写清
- [x] findings evidence-based、adversarial、可执行，无 style/nit/speculation（2 个 nit 标注为不阻塞）
- [x] open questions / residual risks 与 findings 分开
- [x] conclusion 为 pass / pass-with-risks / fail
- [x] output path 使用本机系统时钟 timestamp，匹配 `docs/reviews/plan-review-[0-9]{8}-[0-9]{6}.md`

## Conclusion

**pass**

Plan 证据充分、scope 级别适当、non-goals 明确、allowed write set 收窄、测试策略覆盖 findings 对应的 residual risk。2 个 implementation nit 不阻塞——实现 agent 可自行调整或按 plan 原文执行。
