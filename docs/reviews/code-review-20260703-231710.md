# Code Review

## Scope

- Mode: current changes
- Branch: main
- Base: main
- Output file: docs/reviews/code-review-20260703-231710.md
- Included scope: all unstaged and staged workspace changes relative to HEAD (which is main)
- Excluded scope: untracked directories `.fund_checklist_cli_smoke/`, `.fund_checklist_cli_smoke_9d/` (test artifacts); `docs/reviews/code-review-20260703-011252.md` (prior review artifact)
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- **non-NOT_FOUND failure mid-routing 无显式测试**：`_run_with_query_candidates` 在候选 query 返回非 `NOT_FOUND` failure（如 `UNAVAILABLE`）时立即返回该失败。实现正确（`reading_service.py:442-443`），但当前无测试覆盖该路径。现有 `_RoutingHost` 只返回 `NOT_FOUND` 或 success。建议后续补充一个使用非 `NOT_FOUND` failure code 的 `_RoutingHost` 变体测试。
- **CLI 测试 fake 缺少 `routing_trace` 字段**：`test_cli_maps_service_agent_failure_to_exit_2`（`test_cli.py:374`）使用 `type()` 动态构造 fake result，不含 `routing_trace`。当前 CLI 只访问 `agent_result` 所以无影响，但若未来 CLI 需要读取 `routing_trace`，该 fake 会静默返回 `None` 而非 `()`。建议后续更新 fake 使用 `ReadLocalReportResult` 或补充 `routing_trace` 属性。

## Verification

- `uv run pytest tests/fund/service/test_reading_service.py tests/fund/cli/test_cli.py` -> 32 passed
- `uv run pytest tests/fund/agent/test_minimal_tool_loop.py tests/fund/document_tools/test_docling_store.py tests/fund/document_tools/test_service.py` -> 26 passed
- `git diff --check` -> clean

## Conclusion

Slice 9E 实现与 design doc、implementation-control 和 AGENTS.md 一致。`QueryRouteAttempt` 严格限定 4 个字段并通过 field set 锁定测试；`routing_trace` 不进入 Agent `tool_trace` 或 CLI 输出；四类路由路径（原始成功、fallback 成功、全部 not_found、非受控 query）均有测试覆盖；旧测试无回退。两个 residual risk 均为测试覆盖建议，不阻塞当前 slice。
