# Plan Review: Slice G — audit_pipeline 章节迭代硬编码收口

## Reviewed Target

- **Plan**: `.sisyphus/plans/audit-chapter-iteration-hardcode-slice-20260815.md`
- **Scope**: `audit_pipeline.py:2681` 唯一剩余 `range(1,7)` 硬编码收口 + 文档/AGENTS.md 同步
- **Source**: F1-F4 快照修复 MiMo diff review ACCEPTED（`docs/reviews/code-review-20260815-122324.md`）

## 核验项（逐项）

### 1. audit_pipeline.py:2681 硬编码存在 — **已验证**

- **Plan 声称**: `audit_pipeline.py:2681` 存在 `for cid in range(1, 7):`，位于 `ReportGenerationCoordinator._generate_chapter_content` 内 LLM 分析摘要注入循环。
- **代码实证**: L2677-2687 确认为 Ch0/Ch7 读取前序章节摘要的注入逻辑（`use_chapter_summaries=True` 时），L2681 当前为 `for cid in self._template.front_chapter_ids:`（已修复），原始状态为 `for cid in range(1, 7):`（与 plan 一致）。
- **结论**: Plan 诊断准确。

### 2. 同文件已用 `self._template.front_chapter_ids`，全 service 目录仅 2681 一处剩余 — **已验证**

- **Plan 声称**: 2135/2138/2148/2195/2471 已用 `self._template.front_chapter_ids`；全 `fund_agent/service/` 仅 2681 一处硬编码。
- **代码实证**:
  - L2135: `for cid in self._template.front_chapter_ids`（阶段 B 并行构造）
  - L2138: `for cid in self._template.front_chapter_ids:`（阶段 B 结果收集）
  - L2148: `for cid in self._template.front_chapter_ids`（阶段 C 全通过检查）
  - L2195: `chapter_summaries={cid: ... for cid in self._template.front_chapter_ids}`（阶段 D 摘要构造）
  - L2471: `for cid in self._template.front_chapter_ids:`（审计上下文摘要注入）
- **全量扫描**: `rg "range(1, [0-9])" fund_agent/service/` 仅命中 L2070（注释，非代码）。L2681 当前已修复为模板驱动。
- **结论**: Plan 声称准确，无遗漏。

### 3. 测试设计有真实区分度 — **已验证**

- **Plan 声称**: quarterly 模板 `front_chapter_ids=(1,2,3,4)`，断言 user_prompt 含 Ch4 摘要、不含 Ch5/Ch6 摘要；旧代码 `range(1,7)` 会注入 Ch5/Ch6 使断言失败，可证伪旧逻辑。
- **代码实证**: `report_template.py:235` quarterly `front_chapter_ids=(1, 2, 3, 4)`；`report_template.py:251` semiannual `front_chapter_ids=(1, 2, 3, 4, 5)`。旧 `range(1,7)` 会额外注入 Ch5/Ch6（quarterly）或 Ch6（semiannual），断言 `不含 Ch5 摘要` 必然失败。
- **结论**: 测试设计有真实区分度，可证伪旧逻辑。

### 4. 文档写点精确性 — **已验证（1 项微调）**

- **design.md §6.25**: 当前标题为 `19 项定案`（L1873），Plan 声称追加第 19 项。实证：F1-F4 修复 slice 已将 18→19，本 slice 应追加第 20 项。Plan 声称「第 19 项」有 off-by-one，需修正为「第 20 项」并更新标题为 `20 项定案`。**NEEDS_FIX（最小修复）**。
- **implementation-control.md**: 当前 L3023 已有 F1-F4 快照修复 slice 记录。Plan 声称追加「F1-F4 快照修复 slice（2026-08-15 收口）」+「Slice G（本次）」。实证：F1-F4 记录已存在，仅需追加 Slice G 记录。Plan 措辞暗示两者都需追加，实际只需 Slice G。**NEEDS_FIX（最小修复）**。
- **AGENTS.md 禁止事项**: 当前 L282-288 无硬编码章节编号禁令。Plan 声称追加，合理。

### 5. CIC-lite 口径 — **已验证**

- **Plan 声称**: 只 implement → tests → diff review；不新增 plan-fix / re-review / evidence gate；不 commit 不 push。
- **AGENTS.md 实证**: L181「禁止新增 plan-fix / re-review / evidence gate」一致。
- **结论**: 口径正确。

### 6. Write set 完整性 — **已验证**

Plan 声称 5 文件：
1. `fund_agent/service/audit_pipeline.py` — L2681 一行改动
2. `tests/fund/service/test_snapshot_report_assembly.py` — 新增测试
3. `docs/design.md` — §6.25 追加
4. `docs/implementation-control.md` — 快照进度节追加
5. `AGENTS.md` — 禁止事项追加

全量 `rg "range(1, [0-9])" fund_agent/service/` 确认无遗漏。write set 无越界。

## Findings

### F1-[低]-off-by-one：design.md §6.25 裁决项编号

- **位置**: Plan「文档」节第 1 点
- **问题类型**: 编号错误
- **当前写法**: 「design.md §6.25 追加第 19 项」
- **实际状态**: F1-F4 修复 slice 已将 18→19（L1873 标题 `19 项定案`），本 slice 应追加第 20 项
- **直接证据**: `docs/design.md:1873` 当前标题 `19 项定案`
- **建议改法**: 「追加第 20 项」+ 标题更新为 `20 项定案`
- **严重程度**: 低（实现 agent 可自行修正编号）

### F2-[低]-implementation-control.md F1-F4 记录已存在

- **位置**: Plan「文档」节第 2 点
- **问题类型**: 冗余描述
- **当前写法**: 「快照 slice 进度节追加记录：F1-F4 快照修复 slice（2026-08-15 收口）... Slice G（本次）」
- **实际状态**: F1-F4 记录已存在于 L3023，仅需追加 Slice G
- **直接证据**: `docs/implementation-control.md:3023` 已有 F1-F4 记录
- **建议改法**: 「仅追加 Slice G 记录；F1-F4 已记录」
- **严重程度**: 低（实现 agent 会发现记录已存在并跳过）

## Residual Risks

1. **L2070 注释残留**: 注释仍引用旧 `range(1, 8)` 语义，非代码，不影响行为。可选清理。
2. **`_generate_chapter_content` 内 `contains_non_year_numbers` 警告**: Plan 已注明 fake 返回不含数字文本即可规避，测试设计合理。

## Verdict

**PASS（2 项低严重度 NEEDS_FIX，不阻塞实施）**。

Plan 实证完备（5 项核验全部通过）、测试设计可证伪、write set 精确无越界、CIC-lite 口径正确。F1/F2 为文档编号/描述微调，实现 agent 可自行修正。
