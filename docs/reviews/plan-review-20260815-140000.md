# Plan Review: Slice G — audit_pipeline 章节迭代硬编码收口

## Reviewed Target

- **Plan**: `.sisyphus/plans/audit-chapter-iteration-hardcode-slice-20260815.md`
- **Scope**: audit_pipeline.py:2681 唯一剩余 `range(1,7)` 硬编码收口 + 文档/AGENTS.md 同步
- **Source**: F1-F4 快照修复 MiMo diff review ACCEPTED（`docs/reviews/code-review-20260815-122324.md`）

## Assumptions Tested（5 项核验）

### 1. audit_pipeline.py:2681 硬编码存在 — **已验证**

`audit_pipeline.py:2681`：
```python
for cid in range(1, 7):
```
位于 `ReportGenerationCoordinator._generate_chapter_content`（L2641 def）内，`use_chapter_summaries=True` 时 Ch0/Ch7 读取前序章节摘要的注入循环。

### 2. 同文件已用 `self._template.front_chapter_ids`，全 service 目录仅 2681 一处剩余 — **已验证**

| 行号 | 用途 |
|------|------|
| 2135 | 阶段 B 并行生成 front 章节 |
| 2138 | 阶段 B 结果收集 |
| 2148 | 阶段 C front 全通过检查 |
| 2195 | 阶段 D closing 章节摘要构造 |
| 2471 | 审计上下文摘要注入 |

全量 `rg "for cid in range\|range(1, [0-9])\|range(0, [0-9])" fund_agent/service/` 结果：
- L2070：注释（`# 解耦前 range(1, 8) 语义`），非代码
- **L2681：唯一剩余硬编码**

### 3. 测试设计有真实区分度 — **已验证**

Plan 设计：quarterly 模板 `front_chapter_ids=(1,2,3,4)`（`report_template.py:235`），断言 user_prompt 含 `### Ch4 摘要`、不含 `### Ch5 摘要` / `### Ch6 摘要`。

- **旧代码** `range(1,7)` 会注入 Ch5/Ch6 → 断言失败 → **测试可证伪旧逻辑**
- **新代码** `self._template.front_chapter_ids` → quarterly 只迭代 1..4 → 断言通过
- SEMIANNUAL 补充断言（front=(1,2,3,4,5)，`report_template.py:251`）含 Ch5、不含 Ch6

### 4. 文档写点精确性 — **已验证**

**design.md §6.25**：当前 18 项裁决（L1873 标题 `18 项定案`），追加第 19 项「章节迭代与摘要注入按模板驱动」，精确无越界。

**implementation-control.md**：快照进度节（L3014 起）记录 Slice 0a/0b + A/B/C/D/E/F + F1-F4 快照修复，追加本 slice 记录，合理。

**AGENTS.md 禁止事项**（L282 起）：当前 5 条禁止事项，追加硬编码章节编号禁令，属于文档闭环。

**Write set 5 文件**：
1. `fund_agent/service/audit_pipeline.py` — L2681 一行改动
2. `tests/fund/service/test_snapshot_report_assembly.py` — 新增测试函数
3. `docs/design.md` — §6.25 追加第 19 项
4. `docs/implementation-control.md` — 快照进度节追加记录
5. `AGENTS.md` — 禁止事项追加一条

5 文件，无越界。Plan 与 plan-review / diff-review artifact 由 controller / reviewer 产出，不在 DS write set，正确。

### 5. CIC-lite 口径 — **已验证**

Plan 明确：implement → tests → diff review；不新增 plan-fix / re-review / evidence gate；不 commit 不 push。与 AGENTS.md CIC-lite 规则一致。

## Findings

无 material findings。

## Residual Risks

1. **Line 2070 注释仍引用 `range(1, 8)`** — 非代码，不影响行为。实现 agent 可选择更新注释，但非必须。
2. **`_generate_chapter_content` 内 `contains_non_year_numbers` 警告** — Plan 已注明 fake 返回不含数字的文本即可规避，测试设计合理。

## Verdict

**PASS**。Plan 实证完备、测试设计可证伪、write set 精确无越界、CIC-lite 口径正确。可进入实施。
