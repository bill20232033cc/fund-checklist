# Diff Review: search excerpt 截断边界（候选 C）

- 日期：2026-08-15
- 审查范围：C slice 4 文件（docling_store.py diff + test_search_excerpt_boundary.py + docs/design.md §6.25 + docs/implementation-control.md）
- Goal：`.sisyphus/goals/search-excerpt-boundary-goal-20260815.md`

## Verdict: ACCEPTED

---

## 逐项核查

### 1. `_align_start_no_number_cut`（docling_store.py:998-1016）

| 行号 | 内容 | 判定 |
|------|------|------|
| 1007 | `if not 0 < start < len(text): return start` | ✓ start==0 或 start>=len 原样返回，边界安全 |
| 1010-1011 | `text[start-1] not in _NUMBER_STRING_CHARS or text[start] not in _NUMBER_STRING_CHARS` | ✓ 两侧均属数字串字符集才判定「内部」，孤立标点不触发 |
| 1013-1014 | `while start > 0 and text[start-1] in _NUMBER_STRING_CHARS: start -= 1` | ✓ 仅向前回退，命中区间不缩短 |

孤立标点验证：`"abc, def"` cut at 4（`,` 后空格）→ `text[3]=','` 在集、`text[4]=' '` 不在集 → 不调整。测试 `test_align_functions_leave_non_number_boundaries_untouched` 覆盖。

### 2. `_align_end_no_number_cut`（docling_store.py:1019-1037）

| 行号 | 内容 | 判定 |
|------|------|------|
| 1028 | `if not 0 < end < len(text): return end` | ✓ end==0 或 end>=len 原样返回 |
| 1031-1032 | `text[end-1] not in _NUMBER_STRING_CHARS or text[end] not in _NUMBER_STRING_CHARS` | ✓ 双侧检查，同 _align_start |
| 1034-1035 | `while end < len(text) and text[end] in _NUMBER_STRING_CHARS: end += 1` | ✓ 仅向后扩展 |

### 3. `_excerpt` 改动（docling_store.py:1040-1051）

| 行号 | 内容 | 判定 |
|------|------|------|
| 1045 | no-hit fallback: `_bounded(text, max_chars)[0]` → `text[:_align_end_no_number_cut(text, max_chars)]` | ✓ 仅 fallback 对齐，_bounded 本身不动 |
| 1049 | `start = _align_start_no_number_cut(text, start)` | ✓ 窗口计算后对齐左边界 |
| 1050 | `end = _align_end_no_number_cut(text, end)` | ✓ 窗口计算后对齐右边界 |

窗口缩短风险：start 对齐仅左移（start 减小），end 对齐仅右移（end 增大），窗口只扩不缩，命中区间始终保留。

### 4. `_search_excerpt` 改动（docling_store.py:1081-1098）

| 行号 | 内容 | 判定 |
|------|------|------|
| 1090 | no-hit fallback 同 _excerpt 改动 | ✓ |
| 1096 | `begin = _align_start_no_number_cut(text, begin)` | ✓ 归一化路径同样对齐 |
| 1097 | `window_end = _align_end_no_number_cut(text, window_end)` | ✓ |

### 5. `_bounded` 未动

- `_bounded` 定义（985-993）不在 diff 中。✓
- `list_sections`（行 152）仍调用 `_bounded(section.text, DEFAULT_SECTION_PREVIEW_CHARS)[0]`。✓
- `read_section`（行 172）仍调用 `_bounded(section.text, ...)`。✓
- 非目标守界：D4 要求不改 `_bounded`，已满足。

### 6. 测试覆盖（test_search_excerpt_boundary.py）

| 测试 | 覆盖点 | 可证伪性 |
|------|--------|----------|
| `test_excerpt_right_edge_aligns_number_string` | 字面路径右边界 | 旧行为末尾 `787,727,` |
| `test_excerpt_left_edge_aligns_number_string` | 字面路径左边界 | 旧行为开头半截数字 |
| `test_search_excerpt_normalized_right_edge_aligns_number_string` | 归一化路径右边界 | 旧行为归一化命中仍切数字 |
| `test_search_excerpt_normalized_left_edge_aligns_number_string` | 归一化路径左边界 | 同上 |
| `test_excerpt_no_hit_fallback_aligns_right_edge` | 无命中 fallback | 旧行为 `text[:240]` 切数字 |
| `test_search_excerpt_no_hit_fallback_aligns_right_edge` | _search_excerpt fallback | 同上 |
| `test_excerpt_window_end_at_number_end_no_extension` | 恰在数字串结束不扩展 | 断言 `== 240` |
| `test_excerpt_window_keeps_bounded_width_when_no_cut` | 非数字边界保持宽度 | 断言 `len == 240` |
| `test_align_functions_leave_non_number_boundaries_untouched` | 孤立标点 + 文本边界 | 断言原样返回 |
| `test_search_document_excerpt_and_snapshot_fallback_capture_full_number` | 集成 search_document → _extract_share_change | 旧行为捕获 `787,727,`；新行为捕获完整 `787,727,758.47` |

全部 10 测试 passed。集成测试通过真实 DoclingDocumentStore 构造长文本，240 字符窗口左/右边缘均切入数字串，验证 excerpt 不含半截数字且快照回退正则捕获完整值。

### 7. 文档同步

- `docs/design.md` §6.25 第 22 项：search excerpt 窗口截断对齐数字串边界裁决。✓
- `docs/implementation-control.md`：候选 C slice 记录。✓

### 8. Write set 边界

仅修改 goal 允许的 4 文件，无越界。✓

### 9. git diff --check

C slice 4 文件干净（无输出）。`test_cli.py:4123` 和 `test_e2e_regression.py:502` 的 blank-at-EOF 为其它 slice 引入，非 C slice 范围。

### 10. 测试回归

| 命令 | 结果 |
|------|------|
| `uv run pytest tests/fund/document_tools/test_search_excerpt_boundary.py tests/fund/document_tools/test_docling_store.py tests/fund/document_tools/test_bm25f_scorer.py -q --tb=short` | 48 passed |
| `uv run pytest tests/fund/service/test_snapshot_extraction.py tests/fund/service/test_snapshot_report_assembly.py -q --tb=short` | 31 passed |

---

## 结论

C slice 实施精确：纯函数对齐逻辑正确（双侧检查避免吞孤立标点、仅扩不缩保护命中区间）、_bounded 未动、测试可证伪、文档同步完整、write set 无越界。无需修复项。
