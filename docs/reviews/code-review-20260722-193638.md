# Code Review — 修复确认（Re-review）

## Scope

- Mode: current changes (re-review of 6-finding fix)
- Branch: main
- Base: HEAD
- Output file: docs/reviews/code-review-20260722-193638.md
- Previous review: docs/reviews/code-review-20260722-190656.md
- Included scope: `extraction.py`, `chapter_generator.py`, `cli/main.py`, `pyproject.toml`, `tests/fund/test_infer_fund_type.py`, `scripts/setup_e2e_data.py`

## 修复逐项确认

### Finding 1 (高): Exception 吞没 → ✅ 已修复

- `extraction.py:1183-1188`: `except (DocumentToolError, Exception): pass` 已拆分为 `except DocumentToolError: pass` + `except Exception: _extraction_error = True`
- `extraction.py:1296-1305`: 新增 fallback 后检查——若 `holdings` 为空且 `_extraction_error` 为 True，返回 `FailureCode.UNAVAILABLE`
- `extraction.py:1453-1457`（资产配置）: 已拆分，Exception 标记为非关键路径
- `extraction.py:1577-1581`（费率抽取）: 已拆分，Exception 标记为非关键路径
- `extraction.py:4457, 4508`（ETF代码提取 helper）: 保持 `except (DocumentToolError, Exception)` — 可接受，该 helper 返回 `None` 表达失败

### Finding 2 (中): fund_type_labels 缺失 → ✅ 已修复

- `chapter_generator.py:528`: 新增 `"index_etf": "ETF", "index_feeder": "联接基金"`

### Finding 3 (中): QDII 列索引兜底 → ✅ 已修复

- `extraction.py:4838-4840`: 删除了 `mapping.setdefault("stock_code", mapping["stock_name"])` 兜底
- `required` 从 `("stock_code", "stock_name", "percentage")` 改为 `("stock_name", "percentage")`
- 新增注释说明原因

### Finding 4 (中): E2E 测试 CI 跳过 → ✅ 已修复

- `pyproject.toml`: 注册 `e2e` marker，注明 CI 前需运行 setup 脚本
- `test_e2e_holdings_regression.py:25`: 添加 `pytestmark = pytest.mark.e2e`
- `scripts/setup_e2e_data.py`: 232 行，覆盖 6 类基金下载+导入+验证

### Finding 5 (低): infer_fund_type 缺测试 → ✅ 已修复

- `tests/fund/test_infer_fund_type.py`: 新建，13 条参数化用例覆盖 5 类基金 × 2+ 命名变体

### Finding 6 (低): setup_e2e_data.py → ✅ 已创建

- `scripts/setup_e2e_data.py`: 232 行，6 类基金 × 5 年 EID 下载 → import → 验证

## 新增 Finding

### 7-未修复-低-`_holdings_column_indexes` required 放宽后调用方契约不匹配

- **入口/函数**: `_holdings_column_indexes` → `_extract_holdings_from_agent_result` / `_extract_qdii_holdings_from_tables`
- **文件(行号)**: `fund_agent/service/extraction.py:4859-4860`
- **输入场景**: 非标表格头含"股票名称"和"占净值比例"但无"股票代码"列
- **实际分支**: `required = ("stock_name", "percentage")` → 返回不含 `stock_code` 的 mapping → 调用方 `column_indexes["stock_code"]` KeyError
- **预期行为**: 旧代码 `required = ("stock_code", "stock_name", "percentage")` → 返回 None → 调用方跳过该表
- **实际行为**: 新代码允许无 stock_code 通过，调用方直接索引访问可能 KeyError
- **直接证据**: `_extract_holdings_from_agent_result` 和 `_extract_qdii_holdings_from_tables` 均使用 `column_indexes["stock_code"]`（非 `.get()`）
- **影响**: 极低概率——A 股持仓表法定要求含代码列；QDII 表经 `_QDII_COLUMN_KEYWORDS` 预筛保证"证券代码"存在
- **建议改法和验证点**: 将调用方的 `column_indexes["stock_code"]` 改为 `column_indexes.get("stock_code", "")`，或保持现状+加注释
- **修复风险（低）**
- **严重程度（低）**: 理论风险，实际触发概率极低

## Open Questions

无

## Residual Risk

- `_extract_qdii_holdings_from_tables` 内部声明了 `_extraction_error = False` 但从未设置为 True（死代码）。不影响功能，但建议清理。
- E2E 测试依赖 EID API 可达性——若 EID 服务不可用，`scripts/setup_e2e_data.py` 会降级为仅使用本地 PDF，但新环境可能无本地 PDF。
