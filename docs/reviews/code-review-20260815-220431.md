# Code Review: 快照 to_context_dict 完整性（候选 B）

- Reviewer: MiMo (controller independent)
- Date: 2026-08-15 22:04:31
- Scope: B slice delta（snapshot_extraction.py to_context_dict + test + docs）
- Verdict: **ACCEPTED**

---

## 1. Code Verification: `snapshot_extraction.py` to_context_dict() (lines 91-125)

### 1.1 Dataclass field count

| Claim | Evidence |
|---|---|
| SnapshotReportData has 22 fields | `dataclasses.fields()` → 22 (verified via Python runtime) |
| to_context_dict() returns 22 keys | `len(data.to_context_dict())` → 22 (verified via Python runtime) |
| key set == field name set | `set(keys) == {f.name for f in dataclasses.fields(SnapshotReportData)}` → True |

### 1.2 New 7 keys (B delta)

| Key | Line | Serialization | Correct? |
|---|---|---|---|
| `fund_code` | 95 | `self.fund_code` (as-is) | ✓ |
| `fund_name` | 96 | `self.fund_name` (as-is) | ✓ |
| `report_year` | 97 | `self.report_year` (as-is) | ✓ |
| `template_id` | 98 | `self.template_id` (as-is) | ✓ |
| `quarter` | 99 | `self.quarter` (as-is, `int | None`) | ✓ |
| `period` | 100 | `self.period` (as-is, `str | None`) | ✓ |
| `citations` | 124 | `[dict(c) for c in self.citations]` | ✓ |

### 1.3 Existing 15 keys (must be unchanged)

| Key | Line | Serialization | Unchanged? |
|---|---|---|---|
| `performance_rows` | 101-109 | list-of-dict (4 explicit fields) | ✓ |
| `scale_info` | 110 | `dict(self.scale_info)` | ✓ |
| `holdings_rows` | 111 | `[dict(r) for r in ...]` | ✓ |
| `allocation_rows` | 112 | `[dict(r) for r in ...]` | ✓ |
| `industry_rows` | 113 | `[dict(r) for r in ...]` | ✓ |
| `share_change` | 114 | `dict(self.share_change)` | ✓ |
| `fund_manager` | 115 | `dict(self.fund_manager)` | ✓ |
| `own_funds` | 116 | `self.own_funds` | ✓ |
| `operation_analysis` | 117 | `self.operation_analysis` | ✓ |
| `financial_rows` | 118 | `[dict(r) for r in ...]` | ✓ |
| `holder_structure` | 119 | `dict(self.holder_structure)` | ✓ |
| `single_investor_20pct` | 120 | `self.single_investor_20pct` | ✓ |
| `risk_notes` | 121 | `self.risk_notes` | ✓ |
| `missing_items` | 122 | `list(self.missing_items)` | ✓ |
| `latest_performance` | 123 | `dict(self.latest_performance)` | ✓ |

**Conclusion**: Pure addition. No existing key changed. Backward compatible. ✓

### 1.4 Style consistency

- `citations` uses `[dict(c) for c in ...]` — identical pattern to `holdings_rows`, `allocation_rows`, `industry_rows`, `financial_rows`. ✓
- Identity fields (`fund_code` etc.) serialized as-is (primitive types, no wrapping). Consistent with `own_funds`, `risk_notes`, etc. ✓

---

## 2. Test Verification: `test_snapshot_to_context_dict_covers_all_dataclass_fields` (lines 126-182)

### 2.1 Fixture design

| Requirement | Evidence |
|---|---|
| `quarter=3` (non-default, default=None) | Line 134: `quarter=3` ✓ |
| `period="H2"` (non-default, default=None) | Line 135: `period="H2"` ✓ |
| `citations` non-empty | Line 153: `citations=({"section_ref": "3.2.1", "table_ref": "table-0002", "page": "5"},)` ✓ |
| All 22 fields populated with distinguishable values | Lines 129-154: every field has a non-default value ✓ |

### 2.2 Falsifiability assertion (line 159)

```python
assert set(context.keys()) == {f.name for f in dataclasses.fields(SnapshotReportData)}
```

This is the key regression guard: if a new field is added to the dataclass but not serialized in `to_context_dict()`, this test will fail. **Falsifiable.** ✓

### 2.3 Per-key value assertion (lines 161-182)

The `_expected` helper (lines 161-179) correctly maps each field's expected serialization:

- `performance_rows` → explicit dict with 4 named fields (matches to_context_dict lines 102-107) ✓
- `holdings_rows`, `allocation_rows`, `industry_rows`, `financial_rows`, `citations` → `[dict(item) for item in value]` ✓
- `scale_info`, `share_change`, `fund_manager`, `holder_structure`, `latest_performance` → `dict(value)` ✓
- `missing_items` → `list(value)` ✓
- All others → passthrough (identity fields, `own_funds`, `operation_analysis`, `single_investor_20pct`, `risk_notes`) ✓

Line 182: `assert context[field.name] == _expected(field.name)` — iterates all 22 fields. ✓

### 2.4 Test result

```
uv run pytest tests/fund/service/test_snapshot_extraction.py -q --tb=short → 9 passed (0.05s)
```

---

## 3. Documentation Verification

### 3.1 `docs/design.md` §6.25 item 21 (line 1898)

Content verified:
- States 22 key serialization scope (correct per runtime verification) ✓
- Lists all 7 new fields by name ✓
- `citations` serialization method described ✓
- Regression guard = `dataclasses.fields` 全集断言 ✓
- Consumer contract unchanged (kwargs, not dict) ✓

### 3.2 `docs/implementation-control.md` (line 3026)

Content verified:
- Records slice as completed (2026-08-15) ✓
- Accurate summary of delta, test, and verification results ✓
- Non-goal boundaries correctly listed ✓

---

## 4. Boundary Check

| Constraint | Status |
|---|---|
| No consumer contract change (identity fields still via kwargs) | ✓ No changes to `extract_snapshot_data` signature or `generate_snapshot_report` call sites |
| No citations wired into generator rendering | ✓ `citations` only serialized, not consumed |
| No failure taxonomy / public tool / CLI / prompts / registry changes | ✓ Only `snapshot_extraction.py` and test touched |
| No candidate C work | ✓ |
| No AGENTS.md changes | ✓ |
| Write set limited to 4 files | ✓ |

---

## 5. Verdict

**ACCEPTED** — B slice is a clean, minimal, falsifiable addition. 7 new keys match dataclass field set exactly. Existing 15 keys unchanged. Test regression guard (`dataclasses.fields` 全集断言) ensures future field additions must sync serialization. Documentation accurately records the contract and rationale.
