# Code Review

## Scope

- Mode: current changes (CIC-lite diff review)
- Branch: main (unstaged workspace changes)
- Base: main
- Output file: docs/reviews/code-review-20260814-160646.md
- Included scope: 本 slice 7 个文件（extraction.py registry aliases 扩展、3 个测试文件、tests/README.md、design.md、implementation-control.md）
- Excluded scope: AGENTS.md / `fund_agent/agent/` / `fund_agent/fund/` / `fund_agent/host/` / `fund_agent/cli/` / `fund_agent/service/models.py` / public tool / 10F/10G/11A 契约
- Parallel review coverage: 无

## Findings

未发现实质性问题。

### 核验记录

**1. registry aliases（extraction.py:267-279）**

`performance_returns` contract aliases 实际内容：
```python
aliases=(
    "净值增长率",
    "业绩比较基准收益率",
    "基准收益率",
    "收益表现",
    "基金净值表现",
    # 2026-08-14 第4个任务：performance 类词面收口。
    # 匹配机制为子串包含（any(alias in query ...)），「超额表现」
    # 经「超额」子串 alias 命中（非「超额收益/超额收益率」）。
    "超额收益",
    "超额收益率",
    "超额",
    "净值表现",
),
```
与 plan 设计口径一致：4 个新增 alias，含匹配语义注释。candidate_queries / acceptable_title_family / anchor_title_family / requires_table_citation 未变。

**2. `_route_plan_for_query` 子串匹配（extraction.py:4768）**

```python
if any(alias in query for alias in contract.aliases):
```
子串包含匹配确认。新增 alias 命中机制与既有逻辑完全复用。

**3. `_ANCHOR_PROFILE_NAMES`（chat_service.py:53）**

```python
_ANCHOR_PROFILE_NAMES = ("manager_holdings", "holdings_top10", "performance_returns")
```
`performance_returns` 已在列，无需修改。锚点注入块（chat_service.py:796-811）使用 `_ANCHOR_PROFILE_NAMES` 检查 + `_resolve_anchor_table_ref` + fail-open 模式，零改动。

**4. diff 与 plan 设计口径一致性**

- registry 扩展仅 aliases，与 plan 一致。
- 锚点注入零新增，复用既有 3.2.1 路径，与 plan 一致。
- 不改 `_resolve_anchor_table_ref`、runner、10F/10G/11A 契约，与 plan 一致。

**5. allowed write set 边界**

实际修改文件 7 个：extraction.py、test_extraction.py、test_chat_service.py、test_e2e_regression.py、tests/README.md、design.md、implementation-control.md — 全部在 plan allowed write set 内，无越界。

**6. 测试覆盖与断言正确性**

- `test_performance_family_excess_return_queries_route_to_performance_returns`：4 个正例（超额收益是多少、超额收益率、超额表现、净值表现如何）→ `performance_returns`，覆盖 plan 测试计划。
- `test_performance_family_extended_aliases_do_not_absorb_other_profiles`：4 个负例（基金规模、持仓明细、管理费、前十大持仓）不误命中。
- `test_performance_family_extended_aliases_keep_registry_valid`：aliases 扩展后 `_validated_locator_contracts()` 不抛 schema_drift，且 4 个新增 alias 均在断言中。
- `test_performance_excess_return_query_injects_anchor`：超额收益 query → performance_returns + 候选表锚点注入，断言完整。
- `test_performance_excess_return_query_anchor_smoke`：参数化真实 fixture（004393-2025 → table-0009、005680-2022 → table-0010），目录缺失 skip。
- 既有 registry aliases 断言已同步更新（新增 4 个 alias 到 expected tuple）。

**7. public tool / 10F/10G/11A 契约**

无改动。

**8. commit/push**

未执行。

## Open Questions

无。

## Residual Risk

- 无。
