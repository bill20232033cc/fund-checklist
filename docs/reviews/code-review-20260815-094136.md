# Code Review

## Scope

- Mode: current changes
- Branch: main
- Base: main (HEAD)
- Output file: docs/reviews/code-review-20260815-094136.md
- Included scope: 当前工作区相对 HEAD 的全部改动（24 个已修改文件 + 16 个 untracked 文件），覆盖 Slice A-E 快照修复的全部代码
- Excluded scope: 无
- Parallel review coverage: 无

## Findings

### 001-未修复-[严重]-risk_notes 字段被错误赋值为 single_investor 文本

- **入口/函数**: `extract_snapshot_data()` → `SnapshotReportData` 构造
- **文件(行号)**: `fund_agent/service/snapshot_extraction.py:206`
- **输入场景**: 所有快照报告生成（季报/半年报，模板模式与 LLM 模式均受影响）
- **实际分支**: `single_investor` 变量（line 159 提取的「单一投资者」文本）同时赋值给 `single_investor_20pct` 和 `risk_notes`
- **预期行为**: `risk_notes` 应包含风险提示文本（如「大额赎回风险」「单一投资者占比过高预警」等），来自独立的风险提示抽取
- **实际行为**: `risk_notes=single_investor`，风险提示字段显示的是单一投资者文本而非风险提示内容；季报 Ch4、半年报 Ch5 的「风险提示」节均显示错误数据
- **直接证据**: line 159 `single_investor = _extract_text_field(tool_service, document_id, _SINGLE_INVESTOR_QUERY, "单一投资者")`，line 206 `risk_notes=single_investor`；无 `_RISK_NOTES_QUERY` 定义，无独立风险抽取逻辑
- **影响**: 数据错误——风险提示章节内容不正确，用户看到的风险信息实为单一投资者信息
- **建议改法和验证点**: (1) 新增 `_RISK_NOTES_QUERY = "风险提示"` 常量；(2) 在 `extract_snapshot_data` 中新增 `risk_notes_text = _extract_text_field(tool_service, document_id, _RISK_NOTES_QUERY, "风险提示")`；(3) 构造 `SnapshotReportData` 时 `risk_notes=risk_notes_text`；(4) 补测试验证 `risk_notes` 与 `single_investor_20pct` 可不同值
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 严重

### 002-未修复-[高]-_extract_share_change 文本回退正则全部使用字面 \\d 而非 \\d

- **入口/函数**: `_extract_share_change()` 文本回退路径
- **文件(行号)**: `fund_agent/service/snapshot_extraction.py:549-553`
- **输入场景**: 当表格路径无法命中份额变动表时（Docling section 归属错位、表缺失等），回退到文本正则匹配
- **实际分支**: `patterns` dict 中 4 个正则均使用字面 `\d`（两字符：反斜杠 + d），`re.search` 永远返回 None
- **预期行为**: 文本回退应从 search 摘要中提取期初/申购/赎回/期末份额数值
- **实际行为**: 4 个正则全部匹配失败，函数返回 `{"ending_shares": "缺失"}`；份额变动数据全部丢失
- **直接证据**: `python3 -c "import re; print(re.search(r'期初s*基金份额总额[^d]{0,8}(d[d,，.]*(?:.d+)?)', '本报告期期初基金份额总额 787,727,758.47'))"` → None；正确 `\d` 模式命中并捕获 `787,727,758.47`
- **影响**: 静默数据丢失——当表格路径未命中时，份额变动 4 个字段全部降级为「缺失」；当前实测表格路径工作正常所以未触发，但文本回退路径完全失效
- **建议改法和验证点**: 将 line 549-553 的 4 个正则中的 `d` 全部改为 `\d`、`s` 改为 `\s`、`[^d]` 改为 `[^\d]`；补一个纯文本回退路径的单元测试（无 table_ref 命中时验证正则提取）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

### 003-未修复-[中]-_period_label 忽略 period 参数，半 hardcode「上半年」

- **入口/函数**: `_period_label()` / `_snapshot_common_header()`
- **文件(行号)**: `fund_agent/service/snapshot_generator.py:17-29`
- **输入场景**: 半年报快照报告生成
- **实际分支**: 函数签名为 `_period_label(template_id, report_year, quarter=None)`，无 `period` 参数；semiannual 恒返回 `f"{report_year} 年上半年"`
- **预期行为**: 若设计支持 H2 半年报，应接受 `period` 参数并映射（H1→上半年、H2→下半年）
- **实际行为**: `_snapshot_common_header` 接收 `period` 参数但不传给 `_period_label`，`period` 值被丢弃
- **直接证据**: line 29 `return f"{report_year} 年上半年"` 无条件分支；`_snapshot_common_header` line 39 接收 `period` 但 line 47 调用 `_period_label(template_id, report_year, quarter)` 未传 period
- **影响**: 当前无功能影响（设计假设 semiannual 仅 H1）；若未来支持 H2 需修改函数签名与映射逻辑
- **建议改法和验证点**: (1) `_period_label` 增加 `period` 参数，semiannual 按 period 映射（H1→上半年、H2→下半年、None→上半年）；(2) `_snapshot_common_header` 透传 period；(3) 补 H2 测试
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 004-未修复-[低]-审计上下文 closing 章节摘要 range(1,7) 硬编码

- **入口/函数**: `ReportGenerationCoordinator._generate_and_audit_chapter_inner()` → 审计上下文组装
- **文件(行号)**: `fund_agent/service/audit_pipeline.py:2471`
- **输入场景**: 快照模板 LLM 模式 closing 章节（ch0 概览）审计
- **实际分支**: `for cid in range(1, 7)` 硬编码 1-6，快照模板 front_chapter_ids 为 (1,2,3,4) 或 (1,2,3,4,5)，恰好是 range(1,7) 子集
- **预期行为**: 应使用 `self._template.front_chapter_ids` 遍历前序章节摘要
- **实际行为**: 当前行为正确（快照 front IDs ⊂ range(1,7)），但若未来模板 front IDs 包含 ≥7 的章节则摘要丢失
- **直接证据**: line 2471 `for cid in range(1, 7)` 与 line 2192 `chapter_summaries={cid: chapter_contents.get(cid, "") for cid in self._template.front_chapter_ids}` 不一致——后者已用模板属性，前者仍硬编码
- **影响**: 当前无功能影响；维护一致性问题
- **建议改法和验证点**: line 2471 改为 `for cid in self._template.front_chapter_ids`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无

## Residual Risk

1. **文本回退路径无测试覆盖**: `test_extract_share_change_semiannual_labels` 仅测试表格路径（search 命中 table_ref），未覆盖纯文本回退场景。正则 bug 在当前测试中不可见。
2. **risk_notes 无独立测试**: 现有测试验证 `missing_items`、`performance_rows` 等字段，但未断言 `risk_notes` 与 `single_investor_20pct` 的值不同。
3. **模板模式端到端验证**: 模板模式（不加 --llm）的完整 Markdown 输出未在测试中验证，包括数据要点摘要渲染与缺失项声明的完整性。
4. **审计管道快照集成**: LLM 模式下审计管道对快照章节的审计行为（prompt 中数据表格不可修改约束 vs 程序化 QNone 表头）未在测试中覆盖。
